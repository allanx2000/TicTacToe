<UserControl x:Class="TicTacToe.Controls.GridButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ToggleButton Margin="0" Padding="0"  Name="Button" 
                  DataContext="{Binding ElementName=Control}" 
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                  IsChecked="{Binding Clicked}" 
                  PreviewMouseLeftButtonDown="OnLeftClick"> 
        <Label Margin="0" Padding="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Viewbox>
                <TextBlock Foreground="{Binding TextColor, FallbackValue=Pink}" Text="{Binding ButtonText, FallbackValue='-'}"/>
            </Viewbox>

        </Label>
    </ToggleButton>
</UserControl>
